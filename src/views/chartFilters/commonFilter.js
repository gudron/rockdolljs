// CommonFilter
// ---------

import Marionette from 'backbone.marionette';

const CommonFilter = Marionette.LayoutView.extend({
    model: false,
    initialize: function () {

    },
    template: false,
    onShow: function () {
        let that = this;

    },
});

export default CommonFilter;